# Web Chat CLI

Интерактивный интерфейс для общения с системой через терминал.

## Принцип работы
Скрипт `src/chat.py` инициирует цикл, считывающий ввод пользователя, отправляющий его в систему обработки (с учетом истории и RAG) и возвращающий ответ.
