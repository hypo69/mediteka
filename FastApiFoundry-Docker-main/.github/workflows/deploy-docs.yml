name: Publish Documentation

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'src/**'
      - 'docs/**'
      - 'mkdocs.yml'
      - 'extensions/**'
      - 'mcp-powershell-servers/**'
      - 'scripts/**'
      - 'install/**'
      - 'utils/**'
      - 'static/**'
      - 'check_engine/**'
      - 'SANDBOX/**'
      - 'config.json'
      - 'config_manager.py'
      - 'run.py'
      - '.github/workflows/deploy-docs.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # ─────────────────────────────────────────────
  # JOB 1: TypeDoc — JS/JSDoc → Markdown
  # ─────────────────────────────────────────────
  docs-js:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Generate JS docs (TypeDoc)
        working-directory: extensions/browser-extension-summarizer
        run: |
          npx --yes typedoc \
            --plugin typedoc-plugin-markdown \
            --out ../../docs/ru/dev/js \
            --name "Browser Extension" \
            --readme README.md \
            --disableSources \
            --hideGenerator \
            --outputFileStrategy modules \
            --flattenOutputFiles \
            summarizer.js connectors/gemini.js \
            connectors/openai-compat.js connectors/openrouter.js \
            logger.js ui-manager.js

      - uses: actions/upload-artifact@v4
        with:
          name: js-docs
          path: docs/ru/dev/js/

  # ─────────────────────────────────────────────
  # JOB 2: PlatyPS — PowerShell comment-based help → Markdown
  # ─────────────────────────────────────────────
  docs-powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate PowerShell docs
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File scripts/generate-ps-docs.ps1

      - uses: actions/upload-artifact@v4
        with:
          name: ps-docs
          path: docs/ru/dev/powershell/

  # ─────────────────────────────────────────────
  # JOB 3: MkDocs deploy (зависит от JS + PS docs)
  # ─────────────────────────────────────────────
  build:
    runs-on: ubuntu-latest
    needs: [docs-js, docs-powershell]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install docs dependencies
        run: pip install -r docs/requirements.txt

      # Скачиваем сгенерированные docs из предыдущих job'ов
      - uses: actions/download-artifact@v4
        with:
          name: js-docs
          path: docs/ru/dev/js/

      - uses: actions/download-artifact@v4
        with:
          name: ps-docs
          path: docs/ru/dev/powershell/

      - name: Build with MkDocs
        run: mkdocs build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
