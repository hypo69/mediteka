# 🚀 DEPLOYMENT WORKFLOW

**Версия:** 1.0.0  
**Проект:** FastAPI Foundry  
**Дата:** 9 декабря 2025  

---

## 📋 АВТОМАТИЧЕСКИЙ WORKFLOW ПОСЛЕ ИЗМЕНЕНИЙ КОДА

### 🔴 ОБЯЗАТЕЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ (БЕЗ ВОПРОСОВ!)

```bash
# 1. ПРОВЕРКА СИНТАКСИСА
python -m py_compile src/**/*.py
python -m flake8 src/ --max-line-length=120

# 2. GIT ОПЕРАЦИИ (АВТОМАТИЧЕСКИ!)
git add .
git commit -m "feat: описание изменений"
git push origin main

# 3. ТЕСТИРОВАНИЕ
python -m pytest tests/ -v
python run.py --test-mode

# 4. ДЕПЛОЙ (если нужен)
# Локальный перезапуск
pkill -f "python run.py"
python run.py &

# 5. ВЕРИФИКАЦИЯ
curl http://localhost:8000/api/v1/health
curl http://localhost:8000/docs
```

---

## ⚡ БЫСТРЫЕ КОМАНДЫ

### После изменения Python кода:
```bash
python -m py_compile {файл} && git add . && git commit -m "fix: {описание}" && git push
```

### После изменения конфигурации:
```bash
python run.py --validate-config && git add . && git commit -m "config: {описание}" && git push
```

### После изменения API:
```bash
python -m pytest tests/test_api.py && git add . && git commit -m "api: {описание}" && git push
```

---

## 🎯 ПРАВИЛА КОММИТОВ

| Тип | Префикс | Пример |
|-----|---------|--------|
| Новая функция | `feat:` | `feat: добавить RAG поиск` |
| Исправление | `fix:` | `fix: ошибка в API endpoint` |
| Конфигурация | `config:` | `config: обновить .env` |
| Документация | `docs:` | `docs: обновить README` |
| Тесты | `test:` | `test: добавить unit тесты` |

---

## 🔧 ПРОВЕРКИ ПЕРЕД КОММИТОМ

✅ Синтаксис Python валиден  
✅ Все импорты работают  
✅ Сервер запускается без ошибок  
✅ API endpoints отвечают  
✅ Конфигурация корректна  

---

## 🚨 КРИТИЧЕСКИЕ ФАЙЛЫ

**НЕ ТРОГАТЬ БЕЗ БЭКАПА:**
- `requirements.txt`
- `.env` (production)
- `src/core/config.py`

**ВСЕГДА ПРОВЕРЯТЬ:**
- `src/api/main.py`
- `src/models/foundry_client.py`
- `run.py`

---

## 📊 МОНИТОРИНГ ПОСЛЕ ДЕПЛОЯ

```bash
# Проверка здоровья
curl http://localhost:8000/api/v1/health

# Проверка моделей
curl http://localhost:8000/api/v1/models

# Проверка логов
tail -f logs/app.log
```

---

**ПОМНИ:** Все операции выполняются АВТОМАТИЧЕСКИ без запросов разрешения!