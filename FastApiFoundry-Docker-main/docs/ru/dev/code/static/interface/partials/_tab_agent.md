#  Tab Agent

**Файл:** `static/interface/partials/_tab_agent.html`  
**Тип:** `.html`

---

**Комментарии:**

> Left: chat area
> Keystroke special panel (shown only for keystroke agent)
> Users training data
> Predict session
> Text input (hidden for keystroke)

**i18n ключи** (23):

`agent.ks_demo`, `agent.ks_panel_title`, `agent.ks_predict`, `agent.ks_session`, `agent.ks_session_hint`, `agent.ks_train`, `agent.ks_users`, `agent.ks_users_hint`, `agent.max_iter`, `agent.model_hint`, `agent.model_placeholder`, `agent.placeholder`, `agent.run`, `agent.select`, `agent.settings`, `agent.start_prompt`, `agent.title`, `agent.tools`, `chat.clear`, `chat.model`, `chat.temperature`, `common.loading`, `common.refresh`


---

*Проект: AI Assistant (ai_assist) · v0.8.0 · автогенерация: `scripts/Create-Doc/Generate-FullReference.py`*
