# API Reference (Autogenerated)

Данный файл обновляется автоматически при каждом запуске сервера в режиме разработки.

## Заголовок отсутствует
Описание отсутствует

**Путь:** `HEAD, GET /openapi.json`

## Заголовок отсутствует
Описание отсутствует

**Путь:** `HEAD, GET /docs`

## Заголовок отсутствует
Описание отсутствует

**Путь:** `HEAD, GET /docs/oauth2-redirect`

## Заголовок отсутствует
Описание отсутствует

**Путь:** `HEAD, GET /redoc`

## None
Serve main interface SPA.

**Путь:** `GET /`

## None
Serve QA test runner page.

**Путь:** `GET /qa`

## None
API Information

**Путь:** `GET /api`

## None
Serve the GUI installer SPA at /install.

**Путь:** `GET /install`

## None
Return basic environment status for the welcome step.

Returns:
    dict â€” python version, pip availability, venv presence, requirements flag.

**Путь:** `GET /api/v1/install/status`

## None
Read current .env values for the env step.

Returns:
    dict â€” exists flag and current key values (tokens masked).

**Путь:** `GET /api/v1/install/env`

## None
Write .env with provided values.

Args:
    body (dict): foundry_url, hf_token, hf_models_dir.

Returns:
    dict â€” success flag.

**Путь:** `POST /api/v1/install/env`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/env \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Check Tesseract OCR installation status.

Returns:
    dict â€” installed flag, version string, executable path.

**Путь:** `GET /api/v1/install/tesseract`

## None
Trigger Tesseract installation via install script.

Returns:
    dict â€” success flag or error message.

**Путь:** `POST /api/v1/install/tesseract/install`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/tesseract/install \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Check Foundry Local installation and service status.

Returns:
    dict â€” installed flag, version, running flag.

**Путь:** `GET /api/v1/install/foundry`

## None
Install Foundry Local via winget.

Returns:
    dict â€” success flag or error message.

**Путь:** `POST /api/v1/install/foundry/install`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/foundry/install \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Check Ollama installation and local server status.

**Путь:** `GET /api/v1/install/ollama`

## None
Install Ollama via scripts/Install/Install-Ollama.ps1.

**Путь:** `POST /api/v1/install/ollama/install`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/ollama/install \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Start the Foundry Local service.

Returns:
    dict â€” success flag or error message.

**Путь:** `POST /api/v1/install/foundry/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/foundry/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Stop the Foundry Local service.

Returns:
    dict â€” success flag or error message.

**Путь:** `POST /api/v1/install/foundry/stop`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/foundry/stop \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return list of available default models to download.

Returns:
    dict â€” available models list.

**Путь:** `GET /api/v1/install/models`

## None
Trigger foundry model load for the selected model.

Args:
    body (dict): model_id â€” Foundry model identifier.

Returns:
    dict â€” success flag or error message.

**Путь:** `POST /api/v1/install/models/download`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/install/models/download \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Restart a background service.

Args:
    service: One of 'foundry', 'llama', 'docs', 'rag'.

Returns:
    dict: success, message.

**Путь:** `POST /api/v1/restart/{service}`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/restart/{service} \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Проверка здоровья сервиса.

All provider blocks share the shape: {status, active_model, ...}

Returns:
    dict: status, foundry_status, llama_status, hf_status, ollama_status,
          docs_status, rag_status, timestamp.

**Путь:** `GET /api/v1/health`

## None
Prepare a model by prefixed model id.

Routes to the correct backend by prefix in the URL path.
For Foundry this performs a warm-up inference request, because Foundry
Local does not expose a reliable HTTP load-into-RAM endpoint.

Args:
    model_id: Provider-prefixed model id, e.g.:
        foundry::qwen3-0.6b-generic-cpu:4
        hf::Qwen/Qwen2.5-0.5B-Instruct
        llama::D:/models/gemma-4-E4B-it-Q4_K_M.gguf
        ollama::qwen2.5:0.5b

Returns:
    dict: success, model_id, provider, status on success; success=False, error on failure.

Examples:
    POST /api/v1/models/foundry::qwen3-0.6b-generic-cpu:4/load
    POST /api/v1/models/hf::Qwen%2FQwen2.5-0.5B-Instruct/load
    POST /api/v1/models/llama::D:%2Fmodels%2Fgemma.gguf/load
    POST /api/v1/models/ollama::qwen2.5:0.5b/load

**Путь:** `POST /api/v1/models/{model_id:path}/load`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/models/{model_id:path}/load \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Unload a model from memory by prefixed model id.

Args:
    model_id: Provider-prefixed model id in the URL path.

Returns:
    dict: success, model_id, provider on success; success=False, error on failure.

Examples:
    POST /api/v1/models/hf::Qwen%2FQwen2.5-0.5B-Instruct/unload
    POST /api/v1/models/foundry::qwen3-0.6b-generic-cpu:4/unload

**Путь:** `POST /api/v1/models/{model_id:path}/unload`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/models/{model_id:path}/unload \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Get all local models from all providers with provider prefixes.

Returns a flat list of all models grouped by provider.
Each model id includes the routing prefix (foundry::, hf::, llama::, ollama::).

Returns:
    dict: success, models (list), count, by_provider (dict with counts per provider).

**Путь:** `GET /api/v1/models`

## None
Get models currently ready/connected across providers.

For Foundry, this uses `foundry service list` through
/foundry/models/loaded so the result is the runtime loaded set.

Returns:
    dict: success, models (list), count.

**Путь:** `GET /api/v1/models/connected`

## None
Get models in OpenAI-compatible format.

Returns a list of all available models from all providers in the OpenAI API format.
The response includes:
- data: Array of model objects with id, object, created, and owned_by fields
- total: Total number of models
- by_provider: Object with model counts per provider

Returns:
    OpenAI-compatible response dictionary

**Путь:** `GET /v1/models`

## None
OpenAI-compatible chat completions endpoint for external tools.

This is intentionally small and routes through the existing backend router,
so OpenCode and other OpenAI-compatible clients can use FastAPI Foundry as
their single local provider.

**Путь:** `POST /v1/chat/completions`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/v1/chat/completions \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Получить статус сервиса Foundry

**Путь:** `GET /api/v1/foundry/status`

## None
Получить список всех доступных моделей

**Путь:** `GET /api/v1/foundry/models/list`

## None
Получение статуса службы Foundry через системный агент.

Returns:
    FoundryStatus: Объект со статусом работы, портом и URL.

**Путь:** `GET /api/v1/foundry/status`

## None
Запуск службы Foundry через системный агент.

Args:
    background_tasks (BackgroundTasks): Фоновые задачи FastAPI.

Returns:
    dict: Статус выполнения и сообщение.

**Путь:** `POST /api/v1/foundry/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Остановка службы Foundry через системный агент.

Returns:
    dict: Сообщение о результате операции.

**Путь:** `POST /api/v1/foundry/stop`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/stop \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Ручной сброс предохранителя для команд Foundry.

**Путь:** `POST /api/v1/foundry/reset-breaker`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/reset-breaker \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Warm up the default model from config.json.

For Foundry, "load" means a tiny inference request that makes the model
ready for normal requests. Foundry Local does not expose a reliable HTTP
load-into-RAM endpoint.

Returns:
    dict: success, model_id, message on success; success=False on failure.

**Путь:** `POST /api/v1/foundry/models/auto-load-default`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/auto-load-default \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Alias for /available.

**Путь:** `GET /api/v1/foundry/models/`

## None
Alias for /available.

**Путь:** `GET /api/v1/foundry/models`

## None
List the full Foundry model catalog via CLI (foundry model list).

This is the catalog of ALL models available for download from Foundry,
not just the ones already cached or loaded.
Uses CLI because Foundry has no HTTP API for the catalog.
Falls back to cached catalog if CLI is unavailable.

Returns:
    dict: success, models (list with id, alias, device, task, size, license, cached),
          count, source ('foundry-cli', 'cached', or 'empty').

**Путь:** `GET /api/v1/foundry/models/catalog`

## None
List models reported by the running Foundry service.

Uses Foundry HTTP API (GET /v1/models).
Returns empty list when Foundry is unreachable (no hardcoded fallback).

NOTE: Foundry Local does not expose a reliable separate "loaded in RAM"
list here. Treat these as available/registered models.

Returns:
    dict: success, models, count, source ('foundry-api', 'cached', or 'empty').

**Путь:** `GET /api/v1/foundry/models/available`

## None
List models downloaded to the local Foundry cache on disk.

Foundry has no HTTP API for listing cached (not-yet-loaded) models,
so this endpoint scans the filesystem directly.

Cache structure: <cache_dir>/Microsoft/<model-dir>/v<version>/

Returns:
    dict: success, models (list of model_id strings), items (full model dicts),
          count, cache_dir.

**Путь:** `GET /api/v1/foundry/models/cached`

## None
List models actually running in the Foundry service.

Uses `foundry service list`, because Foundry's OpenAI-compatible
`GET /v1/models` reports available/registered models, not the runtime
loaded set.

Returns:
    dict: success, models (list of {id, name, status}), count.

**Путь:** `GET /api/v1/foundry/models/loaded`

## None
Download a model to the Foundry cache via CLI.

Foundry has no HTTP API for downloading models — CLI is the only option.
Launches download in background and returns PID immediately.

Args:
    request: {"model_id": "qwen3-0.6b-generic-cpu:4"}

Returns:
    dict: success, model_id, status ('downloading'/'already_cached'), pid.

**Путь:** `POST /api/v1/foundry/models/download`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/download \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Check status of a background download process.

Args:
    pid: PID returned by /download.

Returns:
    dict: success, pid, model_id, status ('downloading'/'done'/'error').

**Путь:** `GET /api/v1/foundry/models/download/status/{pid}`

## None
Warm up a model in Foundry.

The client sends a short chat completion request. This is intentionally
simpler and more reliable than trying to track an internal loaded state.

Args:
    request: {"model_id": "qwen3-0.6b-generic-cpu:4"}

Returns:
    dict: success, model_id, message on success; success=False, error on failure.

**Путь:** `POST /api/v1/foundry/models/load`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/load \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Best-effort unload of a Foundry model.

Args:
    request: {"model_id": "qwen3-0.6b-generic-cpu:4"}

Returns:
    dict: success, model_id, message on success; success=False, error on failure.

**Путь:** `POST /api/v1/foundry/models/unload`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/unload \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Get model status: running in service and/or cached on disk.

Args:
    model_id: Model identifier (path parameter).

Returns:
    dict: success, model_id, loaded (bool), cached (bool), status.

**Путь:** `GET /api/v1/foundry/models/status/{model_id:path}`

## None
Force refresh the model catalog cache from Foundry CLI.

Returns:
    dict: success, models, count, source ('foundry-cli' or error reason).

**Путь:** `POST /api/v1/foundry/models/catalog/refresh`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/catalog/refresh \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Text completion через Foundry /v1/completions.

Args:
    request: {"prompt": str, "model": str (optional),
              "temperature": float, "max_tokens": int, ...}

Returns:
    dict: OpenAI-совместимый ответ или success=False.

**Путь:** `POST /api/v1/foundry/models/completions`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/completions \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Эмбеддинги через Foundry /v1/embeddings.

Args:
    request: {"input": str | list[str], "model": str (optional)}

Returns:
    dict: success, data (list of embedding vectors), usage.

**Путь:** `POST /api/v1/foundry/models/embeddings`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/foundry/models/embeddings \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Generate text via the AI Assistant orchestrator.

Routes to the correct backend based on model prefix:
foundry:: / hf:: / llama:: / ollama:: / lmstudio::

Args:
    request: JSON body with fields:
        prompt (str):                 Input text (required).
        model (str):                  Model ID with prefix, e.g. 'foundry::qwen3-0.6b'.
        temperature (float):          Generation temperature (default: 0.7).
        max_tokens (int):             Max tokens (default: 1000).
        use_rag (bool):               Inject RAG context (default: False).
        top_k (int):                  RAG results count (default: from config).
        translate_model_dialog (bool): Translate prompt→EN and response→user lang.
        user_language (str|null):     User language ISO 639-1. null = auto-detect.

Returns:
    dict: success, content, model, usage, user_language, translated (bool)

**Путь:** `POST /api/v1/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Generate text via AI Assistant orchestrator (all backends).

Model prefix determines backend:
foundry:: / hf:: / llama:: / ollama::

**Путь:** `POST /api/v1/ai/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ai/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Стриминговая генерация текста (Foundry only).

**Путь:** `POST /api/v1/ai/generate/stream`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ai/generate/stream \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Chat with message history via AI Assistant orchestrator (all backends).

**Путь:** `POST /api/v1/ai/chat`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ai/chat \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Стриминговый чат с автоматическим сохранением истории после завершения.

**Путь:** `POST /api/v1/ai/chat/stream`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ai/chat/stream \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Оптимизация параметров генерации — не реализовано.

**Путь:** `POST /api/v1/ai/optimize`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ai/optimize \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Start a new chat session.

Args:
    request: JSON body with fields:
        model (str): Model ID (default: 'default').

Returns:
    dict: success, session_id (UUID), model, message.

**Путь:** `POST /api/v1/chat/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/chat/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Send a message to an existing chat session.

Args:
    request: JSON body with fields:
        session_id (str):    Session ID (required).
        message (str):       Message text (required).
        model (str):         Model ID (optional).
        temperature (float): Sampling temperature (default: 0.7).
        max_tokens (int):    Max tokens (default: 2048).
        source_lang (str):   Input language (default: 'auto').
        locale (str):        Reply language override ('ru', 'he', etc.).

Returns:
    dict: success, response, session_id.

Raises:
    HTTPException 400: Invalid session_id or empty message.
    HTTPException 500: Generation error.

**Путь:** `POST /api/v1/chat/message`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/chat/message \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Send a message with SSE streaming response.

Args:
    request: JSON body with fields:
        session_id (str):    Session ID (required).
        message (str):       Message text (required).
        model (str):         Model ID (optional).
        temperature (float): Sampling temperature (default: 0.7).
        max_tokens (int):    Max tokens (default: 2048).

Returns:
    StreamingResponse: SSE stream with {chunk} events and final {done: True}.

Raises:
    HTTPException 400: Invalid session_id or empty message.

**Путь:** `POST /api/v1/chat/stream`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/chat/stream \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Get in-memory session history.

Args:
    session_id: Chat session UUID.

Returns:
    dict: success, session_id, history (list of {role, content}).

Raises:
    HTTPException 404: Session not found.

**Путь:** `GET /api/v1/chat/history/{session_id}`

## None
Delete an in-memory chat session.

Args:
    session_id: Chat session UUID.

Returns:
    dict: success, message.

Raises:
    HTTPException 404: Session not found.

**Путь:** `DELETE /api/v1/chat/session/{session_id}`

## None
Persist chat history to disk.

Saves to config.dir_dialogs (~/.ai-assist/dialogs/ by default).

Args:
    request: JSON body with fields:
        messages (list):  List of {role, content} (required).
        session_id (str): Session UUID (generated if empty).
        model (str):      Model ID (optional).
        title (str):      Dialog title (optional).
        aborted (bool):   Whether chat was aborted (default: False).

Returns:
    dict: success, file (absolute path), session_id.

Raises:
    HTTPException 400: messages missing or empty.

**Путь:** `POST /api/v1/chat/history/save`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/chat/history/save \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
List saved dialog files from disk, newest first.

Args:
    limit (int):  Max number of entries to return (default: 50).
    offset (int): Pagination offset (default: 0).

Returns:
    dict: success, dialogs (list of metadata), total, dir.

**Путь:** `GET /api/v1/chat/history/list`

## None
Load a single saved dialog from disk.

Args:
    filename: JSON filename (e.g. 'uuid_1234567890.json').

Returns:
    dict: success + full dialog payload.

Raises:
    HTTPException 400: Unsafe filename.
    HTTPException 404: File not found.

**Путь:** `GET /api/v1/chat/history/file/{filename}`

## None
Delete old and oversized dialog files.

Applies retention_days and max_size_mb from config.dialogs.
Can be overridden per-request.

Args:
    request: Optional JSON body with fields:
        retention_days (int): Override retention period.
        max_size_mb (int):    Override size limit.

Returns:
    dict: success, deleted (count), freed_bytes, remaining.

**Путь:** `POST /api/v1/chat/history/cleanup`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/chat/history/cleanup \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
List chat models without probing Foundry.

Returns:
    dict: success, models (list of {id, name, type, size}), count.

**Путь:** `GET /api/v1/chat/models`

## None
Translate text using the configured provider.

Args:
    request: JSON body with fields:
        text (str):          Source text (required).
        source_lang (str):   ISO 639-1 code or 'auto' (default: 'auto').
        target_lang (str):   ISO 639-1 target code (default: 'en').
        provider (str):      Override provider (default: from config).
        api_key (str):       Optional API key override.

Returns:
    dict: success, translated, provider, source_lang, target_lang,
          elapsed_ms, error.

**Путь:** `POST /api/v1/translate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/translate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Detect language of the given text.

Args:
    request: JSON body with fields:
        text (str): Text to detect language for (required).

Returns:
    dict: success, language (ISO 639-1), language_name, error.

**Путь:** `POST /api/v1/translate/detect`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/translate/detect \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return current translator configuration (no secrets).

Returns:
    dict: enabled, default_provider, available_providers.

**Путь:** `GET /api/v1/translate/config`

## None
Получение статуса системы RAG.

Returns:
    dict: Информация о состоянии: enabled, index_dir, model, chunk_size, total_chunks.

Example:
    >>> status = await get_rag_status()
    >>> print(status['enabled'])
    True

**Путь:** `GET /api/v1/rag/status`

## None
Обновление конфигурации системы RAG.

Args:
    config (RAGConfig): Модель данных с новыми настройками.

Returns:
    dict: Статус выполнения операции.

**Путь:** `PUT /api/v1/rag/config`

## None
Выполнение поиска в системе RAG.

Args:
    request (RAGSearchRequest): Запрос с текстом и параметром top_k.

Returns:
    dict: Список результатов с контентом и оценками схожести.

Example:
    >>> res = await search_rag(RAGSearchRequest(query="test", top_k=5, min_score=0.5))
    >>> len(res['results'])
    3

**Путь:** `POST /api/v1/rag/search`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/search \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Run retrieval + prompt + generation with optional SSE streaming.

**Путь:** `POST /api/v1/rag/query`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/query \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Пересборка векторного индекса.

Returns:
    dict: Статус запуска процесса пересборки.

**Путь:** `POST /api/v1/rag/rebuild`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/rebuild \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Индексация одного документа или архива через RAGPipeline.

Поддерживаемые форматы архивов: zip, tar, tar.gz, tgz, 7z, rar.
Содержимое архива рекурсивно извлекается и индексируется как единый документ.

**Путь:** `POST /api/v1/rag/index`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/index \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Индексация одного файла/архива с SSE-прогрессом по всему пайплайну.

Шлёт события вида:
  {"stage": "extract", "message": "..."}
  {"stage": "embed",   "done": 12, "total": 50, "message": "..."}
  {"stage": "index",   "done": true, "message": "..."}
  {"stage": "done",    "success": true, "chunks": 50, "chars": 12345}
  {"stage": "error",   "message": "..."}

Args:
    file (UploadFile): Файл для индексации.

Returns:
    StreamingResponse: text/event-stream с JSON-событиями.

**Путь:** `POST /api/v1/rag/index/stream`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/index/stream \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Пакетная индексация нескольких файлов или архивов за один запрос.

Args:
    files (List[UploadFile]): Список файлов. Поддерживаются все форматы
        включая zip, tar, tar.gz, tgz, 7z, rar.

Returns:
    dict: success, indexed, total, results, errors.

Example:
    POST /api/v1/rag/index/batch
    Content-Type: multipart/form-data
    files=@docs.zip&files=@manual.pdf

**Путь:** `POST /api/v1/rag/index/batch`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/index/batch \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Очистка файлов индекса в настроенной директории.

Returns:
    dict: Количество удаленных файлов.

**Путь:** `POST /api/v1/rag/clear`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/clear \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Получение списка директорий, доступных для индексации.

Returns:
    dict: Список путей с количеством найденных текстовых файлов.

**Путь:** `GET /api/v1/rag/dirs`

## None
Получение текущей рабочей директории сервера.

**Путь:** `GET /api/v1/rag/cwd`

## None
Просмотр файловой системы для выбора папок.

Args:
    path (str): Абсолютный путь для обзора. По умолчанию домашняя папка.

Returns:
    dict: Список вложенных папок и информация о текущем пути.

Raises:
    HTTPException: Если путь не существует или не является директорией.

**Путь:** `GET /api/v1/rag/browse`

## None
Получение списка всех профилей RAG в директории ~/.aiassistant/rag/.

Returns:
    dict: success, profiles.

**Путь:** `GET /api/v1/rag/profiles`

## None
Переключение активного профиля RAG.

Args:
    request (dict): Тело запроса с полем 'name'.

Returns:
    dict: Результат загрузки и путь к новому индексу.

**Путь:** `POST /api/v1/rag/profiles/load`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/profiles/load \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Подключить RAG профиль и сделать его активным.

**Путь:** `POST /api/v1/rag/profiles/{name}/activate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/profiles/{name}/activate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Отключить использование RAG без удаления индексов с диска.

**Путь:** `POST /api/v1/rag/profiles/deactivate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/profiles/deactivate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Удаление профиля RAG с диска.

Args:
    name (str): Имя профиля.

Returns:
    dict: Статус удаления.

**Путь:** `DELETE /api/v1/rag/profiles/{name}`

## None
Создание векторного индекса из локальной директории.

Args:
    request (RAGBuildRequest): Параметры сборки (путь, модель, размер чанка).

Returns:
    dict: Статистика сборки: количество сегментов, признак пересборки.

**Путь:** `POST /api/v1/rag/build`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/build \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Извлечение текста из загруженного файла.

Args:
    file (UploadFile): Файл для обработки.

Returns:
    dict: Извлеченный текст и метаданные.

**Путь:** `POST /api/v1/rag/extract/file`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/extract/file \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Извлечение контента с веб-страницы.

Args:
    request (ExtractURLRequest): URL и параметры парсинга (JS, изображения).

Returns:
    dict: Текстовое содержимое страницы.

**Путь:** `POST /api/v1/rag/extract/url`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/extract/url \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Получение списка поддерживаемых форматов для извлечения текста.

Returns:
    dict: Список расширений.

**Путь:** `GET /api/v1/rag/extract/formats`

## None
Список всех документов в хранилище.

Returns:
    dict: success, documents — список с id, title, chunk_count, updated_at.

**Путь:** `GET /api/v1/rag/documents`

## None
Добавить документ и проиндексировать его инкрементально.

Args:
    request (DocumentAddRequest): title, content, source_path.

Returns:
    dict: success, doc_id, chunks_added.

Example:
    POST /api/v1/rag/documents
    {"title": "Manual", "content": "..."}

**Путь:** `POST /api/v1/rag/documents`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/documents \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Получить документ по id.

Args:
    doc_id (int): Document id.

Returns:
    dict: success, document.

**Путь:** `GET /api/v1/rag/documents/{doc_id}`

## None
Обновить документ и переиндексировать если контент изменился.

Args:
    doc_id (int): Document id.
    request (DocumentUpdateRequest): title, content.

Returns:
    dict: success, chunks_added, changed.

**Путь:** `PUT /api/v1/rag/documents/{doc_id}`

## None
Удалить документ и деактивировать его чанки.

Args:
    doc_id (int): Document id.

Returns:
    dict: success.

**Путь:** `DELETE /api/v1/rag/documents/{doc_id}`

## None
Принудительно переиндексировать один документ.

Args:
    doc_id (int): Document id.

Returns:
    dict: success, chunks_added.

**Путь:** `POST /api/v1/rag/documents/{doc_id}/reindex`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/documents/{doc_id}/reindex \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Перестроить FAISS индекс из активных чанков (удалить мёртвые векторы).

Returns:
    dict: success, vectors_before, vectors_after.

**Путь:** `POST /api/v1/rag/compact`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/compact \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Migrate legacy FAISS/chunks.json profile to SQLite-backed IndexIDMap.

**Путь:** `POST /api/v1/rag/migrate/index-id-map`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/rag/migrate/index-id-map \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Статистика хранилища документов и FAISS индекса.

Returns:
    dict: documents, active_chunks, inactive_chunks, faiss_vectors, compact_recommended.

**Путь:** `GET /api/v1/rag/documents/stats`

## None
Return configured settings and OpenCode server health/version.

**Путь:** `GET /api/v1/opencode/status`

## None
Start `opencode serve` using saved settings.

**Путь:** `POST /api/v1/opencode/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/opencode/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Stop an OpenCode server process started by this API.

**Путь:** `POST /api/v1/opencode/stop`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/opencode/stop \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Proxy OpenCode `GET /config`.

**Путь:** `GET /api/v1/opencode/config`

## None
Return the project-local OpenCode config generated from config.json.

**Путь:** `GET /api/v1/opencode/generated-config`

## None
Proxy OpenCode `PATCH /config`.

**Путь:** `PATCH /api/v1/opencode/config`

## None
Proxy OpenCode provider list.

**Путь:** `GET /api/v1/opencode/providers`

## None
Proxy OpenCode session list.

**Путь:** `GET /api/v1/opencode/sessions`

## None
Create an OpenCode session.

**Путь:** `POST /api/v1/opencode/sessions`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/opencode/sessions \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
List messages for an OpenCode session.

**Путь:** `GET /api/v1/opencode/sessions/{session_id}/messages`

## None
Send a prompt to OpenCode and wait for the response.

**Путь:** `POST /api/v1/opencode/message`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/opencode/message \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return the OpenCode OpenAPI documentation URL.

**Путь:** `GET /api/v1/opencode/openapi-url`

## None
Получить конфигурацию для веб-интерфейса

**Путь:** `GET /api/v1/config`

## None
Частичное обновление конфигурации. Поддерживает dot-notation: 'foundry_ai.default_model'

**Путь:** `PATCH /api/v1/config`

## None
Сохранить конфигурацию

**Путь:** `POST /api/v1/config`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Чтение .env файла как сырой текст для редактора

**Путь:** `GET /api/v1/config/env-raw`

## None
Запись .env файла из редактора (сырой текст)

**Путь:** `POST /api/v1/config/env-raw`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/env-raw \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Чтение config.json как сырой текст для редактора

**Путь:** `GET /api/v1/config/raw`

## None
Запись config.json из редактора (сырой текст).
Валидация JSON на стороне клиента, но проверяем и здесь.

**Путь:** `POST /api/v1/config/raw`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/raw \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Сохранение одной переменной окружения в .env файл.

Используется для сохранения секретов (токены, ключи) из веб-интерфейса.
Переменная обновляется если существует, добавляется если нет.

**Путь:** `POST /api/v1/config/env`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/env \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Экспорт ВСЕХ настроек проекта в один JSON: config.json + .env + MCP конфиги

**Путь:** `GET /api/v1/config/export`

## None
Читает ключи провайдеров из .env. Возвращает замаскированные значения.

**Путь:** `GET /api/v1/config/provider-keys`

## None
Сохраняет ключи провайдеров в .env.

**Путь:** `POST /api/v1/config/provider-keys`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/provider-keys \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Экспортирует ключи провайдеров в формат расширения (chrome.storage.sync).

**Путь:** `GET /api/v1/config/extension-export`

## None
Импортирует ключи из формата расширения (v1/v2) в .env.
providerKeys может содержать строки (app) или массивы (extension).
Неизвестные поля (summaryLang, providerModels и т.д.) игнорируются.

**Путь:** `POST /api/v1/config/extension-import`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/extension-import \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Proxy: fetch model list from external provider API server-side to avoid CORS.

**Путь:** `GET /api/v1/config/provider-models/{provider}`

## None
Импорт полного бэкапа настроек. merge=True — слияние, False — полная замена

**Путь:** `POST /api/v1/config/import`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/config/import \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return available log files with size info.

**Путь:** `GET /api/v1/logs/files`

## None
Return log storage and retention settings.

**Путь:** `GET /api/v1/logs/settings`

## None
Return simple warning/error metrics for the settings page.

**Путь:** `GET /api/v1/logs/health`

## None
Persist log viewer retention settings to config.json and apply them.

**Путь:** `POST /api/v1/logs/settings`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/logs/settings \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return filtered log lines from the requested file.

**Путь:** `GET /api/v1/logs`

## None
Truncate a log file.

**Путь:** `POST /api/v1/logs/clear`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/logs/clear \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Download a log file.

**Путь:** `GET /api/v1/logs/download`

## None
Список моделей пользователя с HuggingFace Hub.

Использует HF_TOKEN из .env.
Возвращает модели пользователя + популярные открытые text-generation модели.

Returns:
    dict: success, username, user_models (list), public_models (list);
          success=False, error, user_models=[], public_models if no token.

**Путь:** `GET /api/v1/hf/hub/models`

## None
Список скачанных и загруженных HF моделей.

Returns:
    dict: success, downloaded (list), loaded (list).

**Путь:** `GET /api/v1/hf/models`

## None
Download model from HuggingFace Hub (blocking, no progress).

Args:
    request: {model_id (str), token (str, optional)}

Returns:
    dict: success, model_id, path on success; success=False, error on failure.

Raises:
    HTTPException 400: model_id not provided.

**Путь:** `POST /api/v1/hf/models/download`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/hf/models/download \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Download HuggingFace model with SSE progress stream.

Streams file-by-file progress events as Server-Sent Events.
Each event is a JSON object with fields:
    type: 'file_start' | 'file_done' | 'done' | 'error'
    filename, file_index, total_files (for file events)
    path (for 'done'), error (for 'error')

Args:
    model_id: HuggingFace model ID, e.g. 'Qwen/Qwen2.5-0.5B-Instruct'.
    token:    Optional HF token (overrides HF_TOKEN env var).

Returns:
    StreamingResponse: text/event-stream

**Путь:** `GET /api/v1/hf/models/download/stream`

## None
Загрузить скачанную модель в память для inference.

Args:
    request: JSON body с полями:
        model_id (str): HuggingFace model ID (обязательно).
        device (str):   'auto', 'cpu', 'cuda' (default: 'auto').

Returns:
    dict: success, model_id, device on success; success=False, error on failure.

Raises:
    HTTPException 400: model_id не передан.

**Путь:** `POST /api/v1/hf/models/load`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/hf/models/load \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Выгрузить модель из памяти (освободить RAM/VRAM).

Args:
    request: JSON body с полями:
        model_id (str): HuggingFace model ID (обязательно).

Returns:
    dict: success, model_id on success; success=False, error on failure.

Raises:
    HTTPException 400: model_id не передан.

**Путь:** `POST /api/v1/hf/models/unload`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/hf/models/unload \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Генерация текста через локальную HF модель.

Args:
    request: JSON body с полями:
        model_id (str):       ID модели (должна быть загружена или скачана).
        prompt (str):         Входной текст.
        max_new_tokens (int): Максимум новых токенов (default: 512).
        temperature (float):  Температура (default: 0.7).

Returns:
    dict: success, content, model on success; success=False, error on failure.

Raises:
    HTTPException 400: model_id или prompt не переданы.

**Путь:** `POST /api/v1/hf/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/hf/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Статус HuggingFace интеграции — доступность библиотек.

Returns:
    dict: success, transformers ({available, version}),
          huggingface_hub ({available, version}),
          torch ({available, version, cuda}),
          hf_token_set (bool), models_dir (str), install_cmd (str).

**Путь:** `GET /api/v1/hf/status`

## None
Статус llama.cpp сервера.

**Путь:** `GET /api/v1/llama/status`

## None
Скопировать .gguf модель в ~/.models.

Если модель уже там есть — пропускает копирование.

Body: {"model_path": "D:/gemma-2-2b-it-Q6_K.gguf"}

**Путь:** `POST /api/v1/llama/models/copy`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/models/copy \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Запустить llama.cpp сервер с указанной GGUF моделью.

Body:
    model_path:   Путь к .gguf файлу или директории с моделями.
                  Если не указан — берётся из config.json (llama_cpp.model_path → directories.models).
                  Если указана директория — берётся первый .gguf файл в ней.
    port:         Порт (default: 9780)
    ctx_size:     Размер контекста (default: 4096)
    threads:      Количество потоков CPU (default: auto)
    n_gpu_layers: Слоёв на GPU, 0 = только CPU (default: 0)
    host:         Хост (default: 127.0.0.1)

**Путь:** `POST /api/v1/llama/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Остановить llama.cpp сервер.

**Путь:** `POST /api/v1/llama/stop`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/stop \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Параметры запущенного llama-server: модель, контекст, потоки, n_gpu_layers.

Проксирует GET /props нативного llama-server API.

Returns:
    dict: success, props (model_path, n_ctx, n_threads, n_gpu_layers, ...)

**Путь:** `GET /api/v1/llama/props`

## None
Активные слоты инференса llama-server.

Проксирует GET /slots нативного llama-server API.
Показывает параллельные запросы в обработке.

Returns:
    dict: success, slots (list)

**Путь:** `GET /api/v1/llama/slots`

## None
Prometheus-метрики llama-server: токены/сек, TTFT, очередь.

Проксирует GET /metrics нативного llama-server API.
Возвращает text/plain в формате Prometheus.

Returns:
    Response: text/plain Prometheus metrics или JSON с ошибкой.

**Путь:** `GET /api/v1/llama/metrics`

## None
Нативная генерация llama-server (быстрее, больше параметров).

Проксирует POST /completion нативного llama-server API.
Поддерживает top_k, repeat_penalty, mirostat и другие параметры
недоступные в OpenAI-совместимом /v1/chat/completions.

Args:
    request: JSON body — любые параметры llama-server /completion.
        prompt (str):          Входной текст (обязательно).
        temperature (float):   Температура (default: 0.7).
        top_k (int):           Top-K sampling (default: 40).
        top_p (float):         Top-P sampling (default: 0.95).
        repeat_penalty (float): Штраф за повторения (default: 1.1).
        n_predict (int):       Макс. токенов (default: 512).
        stop (list[str]):      Стоп-последовательности.
        stream (bool):         Стриминг (default: false).

Returns:
    dict: success, content, tokens_predicted, tokens_evaluated, ...

**Путь:** `POST /api/v1/llama/completion`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/completion \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Токенизация текста через llama-server.

Проксирует POST /tokenize нативного llama-server API.

Args:
    request: {"content": "текст для токенизации"}

Returns:
    dict: success, tokens (list[int])

**Путь:** `POST /api/v1/llama/tokenize`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/tokenize \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Детокенизация (токены → текст) через llama-server.

Проксирует POST /detokenize нативного llama-server API.

Args:
    request: {"tokens": [1, 2, 3, ...]}

Returns:
    dict: success, content (str)

**Путь:** `POST /api/v1/llama/detokenize`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/detokenize \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
OpenAI-совместимый text completion через llama-server.

Проксирует POST /v1/completions llama-server API.

Args:
    request: OpenAI completions body (prompt, max_tokens, temperature, ...).

Returns:
    dict: OpenAI-совместимый ответ.

**Путь:** `POST /api/v1/llama/v1/completions`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/v1/completions \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Получить эмбеддинги через llama-server.

Проксирует POST /v1/embeddings llama-server API.
Требует запуска llama-server с флагом --embedding.

Args:
    request: {"input": "текст или список текстов", "model": "..."}

Returns:
    dict: success, data (list of embedding vectors), usage.

**Путь:** `POST /api/v1/llama/v1/embeddings`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/v1/embeddings \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
OpenAI-совместимый chat completion через llama-server.

Проксирует POST /v1/chat/completions llama-server API.
Используется router.py для генерации через llama:: префикс.

Args:
    request: OpenAI chat completions body (messages, model, temperature, ...).

Returns:
    dict: OpenAI-совместимый ответ.

**Путь:** `POST /api/v1/llama/v1/chat/completions`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/llama/v1/chat/completions \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Список моделей загруженных в llama-server (OpenAI-совместимый).

Проксирует GET /v1/models llama-server API.

Returns:
    dict: success, data (list of model objects), model_name (str, первая модель).

**Путь:** `GET /api/v1/llama/v1/models`

## None
Сканировать .gguf файлы в ~/.models, ~/.ai-assist/gguf_models, ~/.lmstudio/models и опциональном extra_dir.

Основные директории: ~/.models, ~/.ai-assist/gguf_models, ~/.lmstudio/models
Дополнительная: extra_dir из query-параметра (например D:\ если модель ещё не скопирована)

**Путь:** `GET /api/v1/llama/models`

## None
Ollama server status — running, version, URL.

Returns:
    dict: success, running (bool), url, openai_url, version.

**Путь:** `GET /api/v1/ollama/status`

## None
List locally available Ollama models.

Returns:
    dict: success, models (list of {name, size_gb, modified_at, digest}), count.

**Путь:** `GET /api/v1/ollama/models`

## None
Pull (download) a model from Ollama Hub.

Args:
    request: JSON body с полями:
        model (str): Model name, e.g. 'qwen2.5:0.5b' (обязательно).

Returns:
    dict: success, model, status on success; success=False, error on failure.

Raises:
    HTTPException 400: model не передан.

**Путь:** `POST /api/v1/ollama/models/pull`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ollama/models/pull \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Delete a local Ollama model to free disk space.

Args:
    request: JSON body с полями:
        model (str): Model name, e.g. 'qwen2.5:0.5b' (обязательно).

Returns:
    dict: success, model on success; success=False, error on failure.

Raises:
    HTTPException 400: model не передан.

**Путь:** `POST /api/v1/ollama/models/delete`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ollama/models/delete \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Generate text via Ollama.

Args:
    request: JSON body с полями:
        model (str):       Model name (обязательно).
        prompt (str):      Input text (обязательно).
        max_tokens (int):  Max tokens to generate (default: 512).
        temperature (float): Sampling temperature (default: 0.7).

Returns:
    dict: success, content, model on success; success=False, error on failure.

Raises:
    HTTPException 400: model или prompt не переданы.

**Путь:** `POST /api/v1/ollama/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/ollama/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
LM Studio server status.

**Путь:** `GET /api/v1/lmstudio/status`

## None
List LM Studio models.

**Путь:** `GET /api/v1/lmstudio/models`

## None
Load a model in LM Studio.

**Путь:** `POST /api/v1/lmstudio/models/load`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/lmstudio/models/load \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Unload a loaded LM Studio model instance.

**Путь:** `POST /api/v1/lmstudio/models/unload`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/lmstudio/models/unload \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Start an LM Studio model download job.

**Путь:** `POST /api/v1/lmstudio/models/download`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/lmstudio/models/download \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Get LM Studio model download status.

**Путь:** `GET /api/v1/lmstudio/models/download/status/{job_id}`

## None
Generate text through LM Studio /api/v1/chat.

**Путь:** `POST /api/v1/lmstudio/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/lmstudio/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
List all MCP servers from settings.json with their status

**Путь:** `GET /api/v1/mcp-powershell/servers`

## None
Start an MCP server by name

**Путь:** `POST /api/v1/mcp-powershell/servers/{name}/start`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/mcp-powershell/servers/{name}/start \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Stop an MCP server by name

**Путь:** `POST /api/v1/mcp-powershell/servers/{name}/stop`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/mcp-powershell/servers/{name}/stop \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Status of a specific MCP server

**Путь:** `GET /api/v1/mcp-powershell/servers/{name}/status`

## None
Get the contents of mcp-powershell-servers/settings.json

**Путь:** `GET /api/v1/mcp-powershell/settings`

## None
Save mcp-powershell-servers/settings.json

**Путь:** `POST /api/v1/mcp-powershell/settings`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/mcp-powershell/settings \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Список зарегистрированных агентов

**Путь:** `GET /api/v1/agent/list`

## None
Список инструментов конкретного агента

**Путь:** `GET /api/v1/agent/{agent_name}/tools`

## None
Запустить агента.

Агент сам решает какие инструменты вызвать для ответа на вопрос.
По умолчанию используется агент 'powershell'.

**Путь:** `POST /api/v1/agent/run`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/agent/run \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Проверить доступность зависимостей конвертера

**Путь:** `GET /api/v1/converter/status`

## None
Конвертировать .gguf файл в ONNX.

- **gguf_path**: путь к .gguf файлу на сервере
- **output_dir**: директория для сохранения результата
- **model_type**: тип модели для оптимизатора (gpt2, bert, bart)
- **opset**: версия ONNX opset (рекомендуется 17)
- **optimize**: запустить оптимизацию после экспорта

**Путь:** `POST /api/v1/converter/convert`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/converter/convert \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
RAM, CPU, disk and GPU usage.

Returns:
    dict: success, ram_used_mb, ram_available_mb, ram_total_mb, ram_pct,
          cpu_pct, disk_used_gb, disk_total_gb, disk_pct,
          proc_ram_mb, proc_cpu_pct, proc_threads,
          gpus (list).

**Путь:** `GET /api/v1/system/stats`

## None
Return all support dialogs grouped by chat_id.

Returns:
    Dict: {success, dialogs: {chat_id: [{role, text, ts, username}]}}

**Путь:** `GET /api/v1/support/dialogs`

## None
Return available RAG profiles.

Returns:
    Dict: {success, profiles: [...]}

**Путь:** `GET /api/v1/support/rag-profiles`

## None
Create a new RAG profile directory.

Args:
    body (Dict): {name, description}

Returns:
    Dict: {success, path}

**Путь:** `POST /api/v1/support/rag-profiles`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/support/rag-profiles \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Soft-delete a RAG profile (rename with ~ suffix).

Args:
    name (str): Profile name.

Returns:
    Dict: {success}

**Путь:** `DELETE /api/v1/support/rag-profiles/{name}`

## None
Return current support bot configuration.

Returns:
    Dict: {success, enabled, rag_profile}

**Путь:** `GET /api/v1/support/config`

## None
Return all helpdesk dialogs grouped by chat_id.

Returns:
    Dict: {success, dialogs: {chat_id: [{role, text, ts, username}]}}

**Путь:** `GET /api/v1/helpdesk/dialogs`

## None
Return available RAG profiles.

Returns:
    Dict: {success, profiles: [...]}

**Путь:** `GET /api/v1/helpdesk/rag-profiles`

## None
Create a new RAG profile directory.

Args:
    body (Dict): {name, description}

Returns:
    Dict: {success, path}

**Путь:** `POST /api/v1/helpdesk/rag-profiles`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/helpdesk/rag-profiles \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Soft-delete a RAG profile (rename with ~ suffix).

Args:
    name (str): Profile name.

Returns:
    Dict: {success}

**Путь:** `DELETE /api/v1/helpdesk/rag-profiles/{name}`

## None
Return current helpdesk bot configuration.

Returns:
    Dict: {success, enabled, rag_profile}

**Путь:** `GET /api/v1/helpdesk/config`

## None
List all MCP tools discovered from local MCP servers.

Returns a flat list of all tools available to the MCP agent,
grouped by server name.

Returns:
    dict: success, tools list, total count.

Example response:
    {
      "success": true,
      "total": 5,
      "tools": [
        {
          "name": "mcp__powershell-stdio__run-script",
          "server": "powershell-stdio",
          "mcp_tool": "run-script",
          "description": "[MCP:powershell-stdio] Run a PowerShell script"
        }
      ]
    }

**Путь:** `GET /api/v1/mcp-agent/tools`

## None
Re-discover tools from all MCP servers in settings.json.

Useful after starting new MCP servers or changing settings.json.
Clears the cached tool list and re-queries each server via tools/list.

Returns:
    dict: success, total discovered tools count.

**Путь:** `POST /api/v1/mcp-agent/refresh-tools`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/mcp-agent/refresh-tools \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
List MCP servers with their discovered tool counts.

Returns:
    dict: success, servers list with tool counts per server.

Example response:
    {
      "success": true,
      "servers": [
        {"name": "powershell-stdio", "tool_count": 3},
        {"name": "local-models", "tool_count": 2}
      ]
    }

**Путь:** `GET /api/v1/mcp-agent/servers`

## None
Add a new QA training pair.

Args:
    request (dict): Fields:
        - question (str): User question.
        - answer_correct (str): Ideal answer.
        - answer_wrong (str, optional): Example of a bad answer.
        - source (str, optional): Origin tag. Default: "manual".
        - metadata (dict, optional): Extra fields.

Returns:
    dict: ``{success, pair}``

**Путь:** `POST /api/v1/training/pairs`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/training/pairs \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Approve a QA pair — mark it ready for training.

Args:
    pair_id (str): UUID of the pair.

Returns:
    dict: ``{success}``

**Путь:** `PATCH /api/v1/training/pairs/{pair_id}/approve`

## None
Reject a QA pair — exclude it from training.

Args:
    pair_id (str): UUID of the pair.

Returns:
    dict: ``{success}``

**Путь:** `PATCH /api/v1/training/pairs/{pair_id}/reject`

## None
Permanently delete a QA pair.

Args:
    pair_id (str): UUID of the pair.

Returns:
    dict: ``{success}``

**Путь:** `DELETE /api/v1/training/pairs/{pair_id}`

## None
List QA pairs with optional filtering.

Args:
    status (str, optional): pending / approved / rejected.
    source (str, optional): Filter by source tag.
    limit (int): Max results. Default 100.
    offset (int): Skip first N. Default 0.

Returns:
    dict: ``{success, pairs, total}``

**Путь:** `GET /api/v1/training/pairs`

## None
Get a single QA pair by ID.

Args:
    pair_id (str): UUID of the pair.

Returns:
    dict: ``{success, pair}``

**Путь:** `GET /api/v1/training/pairs/{pair_id}`

## None
Return dataset statistics.

Returns:
    dict: ``{success, stats}`` where stats has total/pending/approved/rejected.

**Путь:** `GET /api/v1/training/stats`

## None
Export all approved QA pairs as JSON.

Returns:
    dict: ``{success, pairs, total}``

**Путь:** `GET /api/v1/training/export`

## None
Launch a fine-tuning run over approved QA pairs.

Args:
    request (dict): Fields:
        - strategy (str): "sft" / "dpo" / "lora". Default: "sft".
        - model_id (str, optional): Base model to fine-tune.
        - output_dir (str, optional): Output directory. Default: "training_output".
        - max_pairs (int, optional): Max approved pairs to use. Default: 10000.

Returns:
    dict: ``{success, result}``

Example:
    >>> # POST /api/v1/training/run
    >>> # {"strategy": "dpo", "model_id": "hf::mistral-7b-instruct"}

**Путь:** `POST /api/v1/training/run`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/training/run \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Record a page view event from the browser extension.

Args:
    event: PageViewEvent with user_id, url, title, time_spent, timestamp.

Returns:
    dict: success status and total views count for the user.

**Путь:** `POST /api/v1/recommender/track`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/recommender/track \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Generate AI-powered recommendations based on browsing history.

Args:
    request: RecommendRequest with user_id, optional model, top_k.

Returns:
    dict: success, answer (recommendations text), tool_calls log.

**Путь:** `POST /api/v1/recommender/recommendations`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/recommender/recommendations \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return filtered browsing history for a user.

Args:
    user_id: User identifier.
    min_time: Minimum seconds on page to include (default 10).

Returns:
    dict: success, user_id, views list, count.

**Путь:** `GET /api/v1/recommender/history`

## None
List locally available content blocks.

**Путь:** `GET /api/v1/content/blocks`

## None
Return a structured content block.

**Путь:** `GET /api/v1/content/blocks/{slug}`

## None
Return a sanitized HTML rendering of a content block.

**Путь:** `GET /api/v1/content/blocks/{slug}/html`

## None
Generate a new cryptographically secure API key for this server.

Generates 32 random bytes encoded as a 64-character hex string.
Writes the key to .env (API_KEY) and config.json (security.api_key).
Sets the key in the current process environment immediately.

Returns:
    dict: ``{"success": True, "api_key": "<new key>"}``

Example:
    >>> POST /api/v1/security/api-key/generate
    {"success": true, "api_key": "a3f1..."}

**Путь:** `POST /api/v1/security/api-key/generate`

### Пример вызова:

```bash
curl -X POST http://localhost:9696/api/v1/security/api-key/generate \
     -H "Content-Type: application/json" \
     -d '{"param": "value"}'
```

## None
Return whether an API key is currently configured.

Never returns the key value itself — only its presence and a masked preview.

Returns:
    dict: ``{"success": True, "configured": bool, "preview": "abcd...ef12"}``

Example:
    >>> GET /api/v1/security/api-key/status
    {"success": true, "configured": true, "preview": "a3f1...cd89"}

**Путь:** `GET /api/v1/security/api-key/status`

## None
Remove the API key, disabling key-based protection.

Removes API_KEY from .env and clears security.api_key in config.json.
Unsets the key from the current process environment.

Returns:
    dict: ``{"success": True, "message": "API key removed"}``

Example:
    >>> DELETE /api/v1/security/api-key
    {"success": true, "message": "API key removed"}

**Путь:** `DELETE /api/v1/security/api-key`
